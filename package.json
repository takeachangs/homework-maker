{
  "name": "homework-maker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve",
    "build": "mkdir -p dist && cp -r *.html *.css *.js robots.txt public dist/",
    "vercel-build": "npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "serve": "^14.0.1"
  }
}
